networks:
  servarr_default:
    name: servarr_default
    ipam:
      config:
        - subnet: 172.39.0.0/24
        
services:
  gluetun:
    container_name: gluetun
    image: qmcgaw/gluetun
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    ports:
      - 8080:8080 # sabnzbd
      - 9696:9696 # prowlarr
    networks:
      servarr_default:
        ipv4_address: 172.39.0.2
    volumes:
      - /opt/docker/servarr/config/gluetun:/config
    env_file:
      - .env
    environment:
      - DNS_ADDRESS=127.0.0.11
    restart: unless-stopped

  jellyseerr:
    container_name: jellyseerr
    image: ghcr.io/fallenbagel/jellyseerr:latest
    init: true
    ports:
      - 5055:5055
    networks:
      servarr_default:
        ipv4_address: 172.39.0.3
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - UMASK=002
      - TZ=${TZ}
    volumes:
      - /opt/docker/servarr/config/jellyseerr:/app/config
    healthcheck:
      test: wget --no-verbose --tries=1 --spider http://localhost:5055/api/v1/status || exit 1
      start_period: 20s
      timeout: 3s
      interval: 15s
      retries: 3
    restart: unless-stopped
    labels:
      - homepage.group=Main
      - homepage.name=Jellyseerr
      - homepage.icon=jellyseerr
      - homepage.href={{HOMEPAGE_VAR_SERVARR_IP}}:5055
      - homepage.weight=4
      - homepage.widget.type=jellyseerr
      - homepage.widget.url={{HOMEPAGE_VAR_SERVARR_IP}}:5055
      - homepage.widget.key={{HOMEPAGE_VAR_JELLYSEERR_API_KEY}}
    
  
  radarr:
    container_name: radarr
    image: lscr.io/linuxserver/radarr:latest
    ports:
      - "7878:7878"
    networks:
      servarr_default:
        ipv4_address: 172.39.0.4
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - UMASK=002
      - TZ=${TZ}
    volumes:
      - /opt/docker/servarr/config/radarr:/config
      - /data:/data
    restart: unless-stopped
    labels:
      - homepage.group=Media Management
      - homepage.name=Radarr
      - homepage.icon=radarr
      - homepage.href={{HOMEPAGE_VAR_SERVARR_IP}}:7878
      - homepage.weight=0
      - homepage.widget.type=radarr
      - homepage.widget.url={{HOMEPAGE_VAR_SERVARR_IP}}:7878
      - homepage.widget.key={{HOMEPAGE_VAR_RADARR_API_KEY}}
    
  sonarr:
    container_name: sonarr
    image: lscr.io/linuxserver/sonarr:latest
    ports:
      - "8989:8989"
    networks:
      servarr_default:
        ipv4_address: 172.39.0.5
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - UMASK=002
      - TZ=${TZ}
    volumes:
      - /opt/docker/servarr/config/sonarr:/config
      - /data:/data
    restart: unless-stopped
    labels:
      - homepage.group=Media Management
      - homepage.name=Sonarr
      - homepage.icon=sonarr
      - homepage.href={{HOMEPAGE_VAR_SERVARR_IP}}:8989
      - homepage.weight=1
      - homepage.widget.type=sonarr
      - homepage.widget.url={{HOMEPAGE_VAR_SERVARR_IP}}:8989
      - homepage.widget.key={{HOMEPAGE_VAR_SONARR_API_KEY}}
    
  lidarr:
    container_name: lidarr
    image: lscr.io/linuxserver/lidarr:nightly
    ports:
      - "8686:8686"
    networks:
      servarr_default:
        ipv4_address: 172.39.0.6
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - UMASK=002
      - TZ=${TZ}
    volumes:
      - /opt/docker/servarr/config/lidarr:/config
      - /data:/data
    restart: unless-stopped
    labels:
      - homepage.group=Media Management
      - homepage.name=Lidarr
      - homepage.icon=lidarr
      - homepage.href={{HOMEPAGE_VAR_SERVARR_IP}}:8686
      - homepage.weight=2
      - homepage.widget.type=lidarr
      - homepage.widget.url={{HOMEPAGE_VAR_SERVARR_IP}}:8686
      - homepage.widget.key={{HOMEPAGE_VAR_LIDARR_API_KEY}}
      
  bazarr:
    container_name: bazarr
    image: lscr.io/linuxserver/bazarr:latest
    ports:
      - "6767:6767"
    networks:
      servarr_default:
        ipv4_address: 172.39.0.8
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - UMASK=002
      - TZ=${TZ}
    volumes:
      - /opt/docker/servarr/config/bazarr:/config
      - /data:/data
    restart: unless-stopped
    labels:
      - homepage.group=Media Management
      - homepage.name=Bazarr
      - homepage.icon=bazarr
      - homepage.href={{HOMEPAGE_VAR_SERVARR_IP}}:6767
      - homepage.weight=3
      - homepage.widget.type=bazarr
      - homepage.widget.url={{HOMEPAGE_VAR_SERVARR_IP}}:6767
      - homepage.widget.key={{HOMEPAGE_VAR_BAZARR_API_KEY}}

  whisparr:
    container_name: whisparr
    image: ghcr.io/hotio/whisparr
    ports:
      - "6969:6969"
    networks:
      servarr_default:
        ipv4_address: 172.39.0.7
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - UMASK=002
      - TZ=${TZ}
    volumes:
      - /opt/docker/servarr/config/whisparr:/config
      - /data:/data
    restart: unless-stopped

  notifiarr:
    container_name: notifiarr
    image: golift/notifiarr
    ports:
      - "5454:5454"
    networks:
      servarr_default:
        ipv4_address: 172.39.0.9
    volumes:
      - /opt/docker/servarr/config/notifiarr:/config
      - /var/run/utmp:/var/run/utmp
      - /etc/machine-id:/etc/machine-id
    restart: unless-stopped

  syncthing:
    container_name: syncthing
    image: lscr.io/linuxserver/syncthing:latest
    ports:
      - 8384:8384
      - 22000:22000/tcp
      - 22000:22000/udp
      - 21027:21027/udp
    networks:
      servarr_default:
        ipv4_address: 172.39.0.10
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - UMASK=002
      - TZ=${TZ}
    volumes:
      - /opt/docker/servarr/config/syncthing:/config
      - /data:/data
    restart: unless-stopped

  prowlarr:
    container_name: prowlarr
    image: lscr.io/linuxserver/prowlarr:latest
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - UMASK=002
      - TZ=${TZ}
    volumes:
      - /opt/docker/servarr/config/prowlarr:/config
    depends_on:
      gluetun:
        condition: service_healthy
        restart: true
    network_mode: service:gluetun
    restart: unless-stopped

  sabnzbd:
    image: lscr.io/linuxserver/sabnzbd:latest
    container_name: sabnzbd
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - UMASK=002
      - TZ=${TZ}
    volumes:
      - /opt/docker/servarr/config/sabnzbd:/config
      - /data:/data
    depends_on:
      gluetun:
        condition: service_healthy
        restart: true
    network_mode: service:gluetun
    restart: unless-stopped